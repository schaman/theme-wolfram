type: object
title: Intro
properties:
  title:      { type: string, title: Title }
  text:       { type: string, title: Text, format: textarea }
  buttontext: { type: string, title: Button text, default: Find Out More }
required: [title, text, buttontext]
additionalProperties: false
